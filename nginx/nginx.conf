# ============================================================================
# Nginx Reverse Proxy Configuration for Pi5 Home Server
# ============================================================================
# This configuration routes requests to different services based on hostname
# All services are accessible via port 80 on the Pi5
# ============================================================================

events {
    worker_connections 1024;
}

http {
    # Basic settings
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    server_tokens off;  # Hide nginx version for security

    # MIME types
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Logging
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log warn;

    # Gzip compression
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript
               application/json application/javascript application/xml+rss;

    # ==========================================================================
    # Connection upgrade map for WebSocket and SSE
    # ==========================================================================
    # This map handles both WebSocket upgrades and keeps connections alive for SSE
    map $http_upgrade $connection_upgrade {
        default upgrade;
        ''      '';  # For SSE, don't send "Connection: upgrade"
    }

    # ==========================================================================
    # GitLab Pipeline Monitor
    # ==========================================================================
    server {
        listen 80;
        server_name gitlab.local gitlab.pi gitlab;

        # Access log for this service
        access_log /var/log/nginx/gitlab-access.log;

        location / {
            proxy_pass http://gitlab-monitor:3000;

            # HTTP/1.1 support for WebSocket and SSE
            proxy_http_version 1.1;

            # WebSocket and SSE support
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;

            # CRITICAL for SSE: Disable buffering
            # Without this, nginx buffers SSE events causing delays
            proxy_buffering off;

            # Disable caching for real-time updates
            proxy_cache off;

            # Tell nginx to not buffer this response
            proxy_set_header X-Accel-Buffering no;

            # Proxy headers
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # Cache bypass for upgraded connections
            proxy_cache_bypass $http_upgrade;

            # Timeouts - increased for SSE long-polling
            # SSE connections can stay open for minutes
            proxy_connect_timeout 60s;
            proxy_send_timeout 300s;      # 5 minutes
            proxy_read_timeout 300s;      # 5 minutes
        }
    }

    # ==========================================================================
    # Homebridge - NOT COMPATIBLE WITH NGINX PROXY
    # ==========================================================================
    # NOTE: Homebridge requires network_mode: host for HomeKit mDNS discovery.
    # This means it cannot be reached via nginx proxy on the bridge network.
    # Access Homebridge directly at: http://192.168.1.10:8581
    # (Replace 192.168.1.10 with your Pi5 IP address)
    #
    # The configuration below will NOT work with host networking:
    # server {
    #     listen 80;
    #     server_name homebridge.local homebridge.pi homebridge;
    #
    #     access_log /var/log/nginx/homebridge-access.log;
    #
    #     location / {
    #         proxy_pass http://homebridge:8581;  # Won't work with host network
    #         proxy_http_version 1.1;
    #         proxy_set_header Upgrade $http_upgrade;
    #         proxy_set_header Connection 'upgrade';
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     }
    # }

    # ==========================================================================
    # Pi-hole Admin Interface (Uncomment when ready)
    # ==========================================================================
    # server {
    #     listen 80;
    #     server_name pihole.local pihole.pi pihole;
    #
    #     access_log /var/log/nginx/pihole-access.log;
    #
    #     location / {
    #         proxy_pass http://pihole:80;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    #
    #     # Pi-hole admin panel
    #     location /admin {
    #         proxy_pass http://pihole:80/admin;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     }
    # }

    # ==========================================================================
    # Notifications Service (Future - for your other app)
    # ==========================================================================
    # server {
    #     listen 80;
    #     server_name notify.local notify.pi notify notifications.local;
    #
    #     access_log /var/log/nginx/notify-access.log;
    #
    #     location / {
    #         proxy_pass http://notifications:3000;
    #         proxy_http_version 1.1;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #     }
    # }

    # ==========================================================================
    # Default server (catch-all)
    # ==========================================================================
    server {
        listen 80 default_server;
        server_name _;

        # Return 404 for unknown hosts
        default_type text/html;
        return 404 '<!DOCTYPE html>
<html>
<head><title>Service Not Found</title></head>
<body>
<h1>404 - Service Not Found</h1>
<p>Available services via nginx:</p>
<ul>
<li>gitlab.local - GitLab Pipeline Monitor</li>
<li>pihole.local - Pi-hole Admin (if enabled)</li>
</ul>
<p>Direct access services (bypass nginx):</p>
<ul>
<li>http://[Pi5-IP]:8581 - Homebridge (requires host network)</li>
<li>http://[Pi5-IP]:9000 - Portainer</li>
</ul>
</body>
</html>';
    }
}
